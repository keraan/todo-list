export const todoFactory = (title, description, dueDate, priority) => {
    const createCard = () => {
        
    }

    return {
        title,
        description,
        dueDate,
        priority,
        createCard,
    }

}